import React from 'react';

const EmptyOption = (props) => {
    return (
        <div className="empty-option">
            <p className="empty-option-text">Please add an option to get started!</p>
        </div>
    );
};

export default EmptyOption;